# Где остановились — 2 сентября 2026

Записка для продолжения. Всё, что ниже, проверено на устройстве или измерено, а
не предполагается. Где проверки не было, это сказано прямо.

## Что выкачено

**0.14.0** — схема приёма с меняющейся дозой (BACKLOG §16). `Medicine.plan`
(`DoseStage[]`) плюс `planFrom`; `stageOn`, `doseChangeOn`, `formatCount` в
`logic/medicines.ts`. Доза берётся на день во всех расчётах: `perTimeOf(med,
day)`, `perDayOf`, `projectedLeft` (доза внутри цикла), `markTakenAt`,
`dosesOn` (нулевая доза — приёмов нет), `doseLine` в напоминаниях. Последний
этап без срока — поддерживающая доза; со сроком — курс заканчивается.
Восстановление переносит схему (`plan()` в io.ts, испорченные этапы
отбрасываются поштучно). Тесты — `tests/plan.test.mjs`.

**Важно про тесты.** Наборы `classify` и `stats`, заявленные в 0.12.0, до
репозитория не доехали: файлы не сохранились, в `tests/run.mjs` их не было. При
восстановлении они сразу поймали два дефекта — восстановление не переносило
схему и категория давления читалась полем `key` вместо `level`. Урок: после
создания набора проверять, что он есть в реестре и что `npm test` печатает его
имя.

**Проверено на приборе 5 сентября:** схема из трёх этапов на живом дневнике —
экран приёма показал «с сегодня по 1 вместо ½», карточка «по 1 за приём ещё 7
дней, дальше по 1½», остаток «≈ 16 шт.». Тестовый препарат убран.

**0.13.1** — выложено на GitHub, проверено на приборе. Шесть аптечных сетей:
параметр поиска у каждой свой (`q` — Аптека.ру, ЕАптека, Мегаптека, Планета
Здоровья; `what` — Здравсити; `query` — Ютека), снят с формы поиска сайта и
проверен на телефоне. Отложенное напоминание снимается по новому признаку
`Reminder.markable`: слот с автосписываемым препаратом не пустеет никогда, и
прежнее правило из 0.12.0 не срабатывало.

**Проверено на Mate 60 Pro 5 сентября на живом дневнике:** «Конкор® Кор 2,5 мг»
открывает нужные упаковки в Планете Здоровья; Здравсити открывается в браузере,
а не перехватывается своим приложением (`<queries>` + `openInBrowser` работают —
у Здравсити на этом телефоне стоит своё приложение, и оно перехватывало);
надгробие из чужого файла удалило препарат из хранилища, измерение пришло, у
источника показана дата «записи по 5 сентября»; отложенное снялось после
отметки приёма (1 → 0). Тестовые данные убраны: 49 измерений, пять препаратов,
один человек.

**Грабля:** проверять адреса аптек через `am start -a VIEW` нельзя — ссылку
перехватывает приложение сети. Форсировать браузер:
`am start -n com.yandex.browser/com.yandex.browser.YandexBrowserMainActivity -a android.intent.action.VIEW -d URL`.

**0.13.0** — тексты и настройка обмена. Закрыта 21 находка линзы текстов: убраны
реклама сахара на «Давлении», абзац приватности на карточке препарата, баннер
календаря в «Аптечке», повтор на «Семье», строка про omblepy; сокращены
«Напоминания» (остались только баннеры с кнопкой), «Копия дневника», отчёт
врачу, «О приложении», инструкция на «Приборе» (сворачивается после выбора
прибора), предупреждение глюкометра (до первой выгрузки). Голос выровнен на
безличный. Обмен: свой файл выбирается на «Семье», имя файла с именем человека
(`backupFilename(now, name)`), чужой файл проверяется при добавлении
(`useFamilySync.addSource`), у источника видна дата последней записи
(`family.freshness`), `describeFamily` не считает свой телефон без файла.

**Измерено в браузере** (`scratchpad/visible-words.mjs` — считает видимые
пояснения: p, .muted, баннеры, шаги, summary): «Приём» 2 слова, корень настроек
0, карточка препарата 11, форма 15, «Аптечка» 19. Тяжёлыми остались «Отчёт
врачу» 236 (документ для врача, там текст уместен) и служебные подэкраны
«История версий» 109, «Пароль копии» 102, «Для таблиц» 102.

**0.12.0** — по разбору 5 сентября (девять линз, 129 агентов, 48 подтверждённых
дефектов, 12 отклонённых, 38 предложений; полный список с доказательствами —
`tasks/wvyvbwzr9.output` в scratchpad сессии, кратко — в плане
`~/.claude/plans/enumerated-discovering-salamander.md`). Сделано:

- P1: семейный обмен применяет чужие надгробия к хранилищу (раньше только
  рапортовал), перечитывает базу перед записью (не затирает введённое во время
  чтения), запись под catch; «К копии дневника» с экрана «Семья» открывает
  копию (подэкран ищется с конца стека); свой файл копий нельзя добавить как
  «телефон семьи»; пароль копии предупреждает про телефоны семьи.
- P2: `tx()` резолвится по `oncomplete`; восстановление переносит `intakeSlots`
  и `pharmacies`; дописанная коробка без свежего штампа; личные кнопки приёма
  не трогают общие часы; `<queries>` в манифесте для браузера; пробное
  напоминание выше диапазона уборки, «Проверить» не трогает канал и
  `lastSound`, отложенное снимается отметкой, «Отложить» с catch; реджекты
  `choose`/`openSource` → `null`; «Поправить остаток» берёт свежий остаток;
  Enter в названии не отправляет форму; двойное «Добавить» в сахаре; отказ
  хранилища при записи давления показывается; календарь из `видимые`;
  геолокация на «Приборе»; `parseJson` не пускает записи без `id`.
- Тесты: `classify.test.mjs`, `stats.test.mjs`, слоты приёма в
  `people.test.mjs`, восстановление слотов/аптек в `io.test.mjs`, лестница
  запросов в `pharmacies.test.mjs`.
- Аптеки: `cleanTradeName` (® → пробел, хвост формы, скобки), `pharmacyQueries`
  — пять ступеней, `pharmacyLinks` с `innHref`; ссылка «по веществу» у
  препарата и в списке покупок; `inn` не обнуляется при правке названия;
  `normalize` в drugs.ts тоже меняет ® на пробел. **Новые сети не добавлены:**
  адреса поиска Здравсити, Ютеки и Планеты Здоровья по догадке не открылись, а
  телефон для проверки был отключён. Кандидаты — в BACKLOG §7в.
- Обзор: `TodayCard` и `ShortageCard` в Medicines.tsx, `Restock` и
  `BackupNudge` вне условия про давление, пустое состояние с кнопками,
  `PeriodPicker` только с данными.
- Уборка P3: удалены `markTaken`, `pharmacyUrl`, `INTAKE_SLOTS`/`setIntakeTime`,
  `Tile`, `shortFormOf`, дубль `monthYear`, `isBpReading`/`isGlucoseReading`,
  восемь мёртвых CSS-правил; `emptyMergeLog()`; `src/logic/when.ts` вместо
  трёх копий `toLocalInput`/`describeWhen`; `GLUCOSE_CONTEXT_ORDER/SHORT` в
  types.ts; поправлены противоречащие комментарии и два тавтологичных теста.
  Не сделано: шесть осиротевших doc-комментариев (types.ts:90, ports.ts).

**Не проверено на приборе** (телефон был отключён всю сессию): удаление по
чужому надгробию через обмен, `<queries>` и открытие ссылки аптеки, «Отложить»
после отметки. Проверено в браузере: все 24 экрана Percy без ошибок, обзор для
«только аптечка», лестница запросов, отсутствие ® в адресах.

**0.11.1** — белый экран при переключении на человека без покупок (хук после
раннего выхода в `Restock`) и второй ряд кнопок людей в аптечке; «Вся семья»
переехала в общую полосу людей и показывается только на «Аптечке».

**0.11.0** — кнопки стандартного приёма стали списком (`IntakeSlot`,
`intakeSlotsOf`, `setIntakeSlots`): добавить, убрать, переименовать. Четыре
старых поля `intakeTimes` переписываются вместе со списком ради копий и сборок,
которые о нём не знают. «Люди» переименованы в «Пользователи», строка настроек
называет содержимое. На экране «Семья» три шага настройки обмена словами.

**В бэклоге две новые задачи:** §16 сложный график приёма (повышение и снижение
дозы) и §17 обмен через WebDAV вместо файлов, с ценой решения.

**0.10.0** — восемь замечаний владельца. Список покупок уходит текстом с
галочками (`FilePort.shareText`), а не файлом «купить.txt»; «Скопировать»
кладёт текст в буфер двумя путями, а при отказе обоих говорит об этом.
В аптечке переключатель человека встал над «Купить» (он и раньше менял его,
стоя ниже) и стал чипом на каждого человека. Фильтр аптечки временной: «на
неделю», «на 2 недели», «на месяц», пороги вложенные. Размер текста переехал
на экран «Экран», а в строках корня названы размер текста и часы приёма —
их не находили.

**0.9.2** — кнопка аптеки открывает поиск по нужному лекарству даже там, где
стоит приложение сети. Проверка на приборе показала, что «Аптека.ру» из списка
покупок открывала это приложение на его главной: у сетей включены «ссылки
приложения» на свой домен, и Android отдаёт им адрес, а разобрать запрос
приложение не берётся. Теперь ссылка уходит через `FilePort.openExternal`,
который на Android спрашивает у системы браузер. Проверено дважды, в том числе
с холодного старта приложения аптеки.

**0.9.1** — видно, когда приложение занято (полоса под шапкой и строка словами:
открытие дневника, чтение записей семьи, сохранение копии, загрузка справочника
лекарств), и свои аптеки: выбранные сети дают кнопки у препарата и в списке
покупок, каждая открывает поиск по названию и дозировке. Адреса проверены
руками — Аптека.ру, ЕАптека, Мегаптека; Здравсити не взят, его поиск по адресу
уводит на главную. Наличия и цены приложение не обещает.

**Percy кончился.** Прогон 0.9.1 вернул «organization has exceeded the limits of
the Percy BrowserStack plan»: снимки ушли, сборка не собралась. За день сделано
шесть прогонов. До сброса лимита визуальную регрессию проверять локальными
скриптами на Playwright (`tools/visual.mjs` отдаёт `SCREENS`, `seed`, `go`), а
Percy звать только перед показом.

**0.9.0** — общий дневник на несколько телефонов, без сервера. Свой дневник
телефон пишет в файл обычной копией, а чужие файлы читает при каждом открытии
приложения: раздел «Семья» в настройках. Правила слияния — в `logic/merge.ts`,
чистые и с тестами: свойства записи берутся из более свежей правки по
`updatedAt`, отметки приёма и свёрнутая история складываются (иначе отметка с
другого телефона пропала бы молча), удаление сильнее правки, расхождение
остатка называется человеку. Люди только добавляются. Копия обновляется по
слепку содержимого (`diarySignature`), а не по числу записей.

Проверено на приборе поверх живого дневника: файл «второго телефона» принёс два
измерения, препарат с отметкой приёма и человека; повторное чтение — «нового не
было»; своя копия после обмена содержит 51 запись. Тестовое убрано.

**Чего обмен не делает:** он не мгновенный (файл переносит облачный клиент, и
когда — решает он) и не идёт при закрытом приложении. Фоновое расписание
потребовало бы нативного рабочего в обеих сборках; владелец решил, что
достаточно обмена при открытии.

**0.8.2** — кнопки, не помещающиеся в строку, стоят столбиком одной ширины
(`row--stack`), а не лесенкой из разных прямоугольников: экран копии дневника,
аптечка, карточка препарата. «Отключить автокопии» стала мельче и уехала под
объяснение — рядом с «Отправить копию» она выглядела равной ей по важности.

**0.8.1** — правки после осмотра подэкранов настроек на телефоне. Проверку
0.8.0 я провёл только на корне, подэкраны не открыл ни разу — и там кнопка
«К настройкам» разваливалась на три строки (`text-wrap: balance` у кнопок),
значок стрелки схлопывался в нулевой размер (правила величины для svg внутри
кнопки в стилях нет), экран копии назывался «Сохранность данных» и ссылался на
исчезнувший раздел «Данные». Теперь подпись возврата одна на всё приложение —
«Назад», и она совпадает с системной кнопкой. Проверено на приборе и в браузере,
обычным и очень крупным текстом.

**0.8.0** — настройки в два уровня и общий стек экранов. Корень: размер текста
с образцом и шесть строк «заголовок · значение · шеврон». На приборе 1,3 экрана
вместо 8,8 (и 12,9 при «Очень крупном»); самый длинный подэкран, копия
дневника, — 1,9 экрана вместо десяти. Навигация стала общей: стек живёт в
приложении, аппаратная «Назад» снимает уровень, а не сворачивает приложение из
середины. Историю WebView для этого не ведём — `canGoBack` состояние, которым
приложение не управляет; в браузере глубина зеркалится в историю ради стрелки и
установленного PWA. Плюс подготовка модели данных к семейному слиянию
(`updatedAt`, идентификатор установки, человек и прибор у измерения, личные цели)
и попутные тексты консилиума. Проверено на приборе, протокол и снимки в хабе.

**0.7.2** — первый человек в новом дневнике получает уникальный номер вместо
`p1`, одинакового на всех установках. Иначе при семейном слиянии отец и жена
стали бы одним человеком, а лечится это только перебиванием идентификаторов,
которое рвёт владельцев коробок и старые копии. Правило про `p1` в
`takesPersonalFrom` осталось наследием: такие копии уже лежат у людей. Посев
Percy заводит человека сам, иначе снимки перестали бы повторяться. Проверено в
браузере на чистой установке; экраны не менялись.

**0.7.1** — семь дефектов из консилиума без изменения экранов: окно части суток
на «Приёме» (`partWindowOpen`), владелец нового препарата из формы, уведомление
на человека с `person` в действии и горизонтом под потолок 450, `parseMedicines`
читает все поля коробки, `mergeRestoredSettings` не затирает людей и настройки
устройства, `pairingKey` вон из снимка, переключатели не рвут слова. Плюс
находки скептиков: честное сообщение после восстановления, предел восьми
человек, чужой пароль копии не становится своим, `fillMissingFromCopy`
дописывает старым коробкам владельца и историю. Проверено на приборе, протокол
в хабе.

**Разборы в хабе** (2 сентября): `консилиум-ux_v1` и
`семейный-аккаунт-и-синхронизация_v2` — оба через панели критиков. План
дальнейшего — `~/.claude/plans/enumerated-discovering-salamander.md`.

**Следующий выпуск.** Крупного плана нет: части А и Б выполнены целиком, из
части В сделано всё, кроме перебивки ключей измерений — она ждёт разведки Э9
(отдаёт ли прибор серийный номер и есть ли у семьи второй тонометр вообще).
Дальше по бэклогу: транспорт семейной синхронизации после разведки облаков
Э1–Э8 и отложенные находки консилиума — обзор «что сегодня», экран «Прибор» с
одной кнопкой, форма препарата, ввод сахара, печать, пустые состояния.

**0.5.1** — люди отдельно от памяти прибора (пункт 9а бэклога), плюс две правки,
найденные проверкой на живом телефоне. Проверено на настоящем дневнике: 49
измерений и пять препаратов пережили обе миграции схемы, владельцы проставились,
переключателя при одном человеке нет.

**0.4.2** — надгробия у удалений, свёрнутая история приёма, кнопка «Найти в
аптеке».

**0.4.1** — раздел «О приложении»: номер версии и история изменений. Источник
один — `CHANGELOG.md` в корне, оттуда же берётся номер текущей версии.
Совпадение с `versionName` в сборке стережёт автопроверка, так что менять версию
надо в двух местах и проверка об этом напомнит.

**0.4.0** — копия дневника в файл, который выбирает человек: Storage Access
Framework, необязательное шифрование паролем, восстановление одной кнопкой.
Собрана, установлена на Mate 60 Pro, весь цикл проверен на живом устройстве.

Прежние релизы лежат на
[странице загрузок](https://github.com/LeonidAn1988/omron-bp/releases) и в хабе
результатов; подпись у всех одна, обновление встаёт поверх и записи сохраняет.

## Состояние телефона

Тестовый прибор — **HUAWEI Mate 60 Pro** (MNA-LX9, Android 15, без сервисов
Google). Mate X3 — личный телефон владельца, к нему не подключаемся.

- Стоит **релизная** сборка 0.11.0 (versionCode 25, 2 сентября 23:20): отладка
  WebView выключена, сокет `webview_devtools_remote` закрыт. **Проверять через
  CDP на ней нельзя** — для проверок ставить отладочную (`assembleDebug`), для
  раздачи семье релизную (`npm run release:android`).
- Ключ подписи у обеих сборок **один и тот же, отладочный** — иначе обновление
  не встало бы поверх дневника с настоящими данными. Свой ключ понадобится к
  магазину, и тогда один раз придётся переустановить с переносом через копию.
- Один человек «Я» (id `pmtizy0g4`, кнопка прибора 1). Тестовые данные проверок
  0.7.1 и 0.8.0 убраны; коробка Метформина восстановлена точно, как была:
  остаток 44, подтверждён 28 августа, отметок нет.
- В аптечке **пять засеянных препаратов** для съёмки экранов. Убрать перед
  передачей.
- Измерений с тонометра — **49, они настоящие**, трогать не надо.
- **Файл автокопий выбран** — `/sdcard/Download/дневник-здоровья-2026-09-01.json`,
  открытый (не зашифрованный). В нём сейчас состояние 14:39 с тестовыми «Отцом»
  и коробками: автокопия переписывается при изменении числа записей, следующее
  добавление или удаление её обновит. Шифрование включалось только на время
  проверки и выключено обратно.

## Копия дневника — как устроено

Три файла, и порядок между ними важен.

| Файл | Что делает |
|---|---|
| `android/.../BackupFile.java` | SAF: `choose` / `write` / `read` / `check` / `forget`. Разрешение долгоживущее, система держит его как `persisted=0x3` |
| `src/logic/crypto.ts` | Конверт PBKDF2-SHA256 210 000 + AES-GCM-256. Неверный пароль отличается от испорченного файла — это не мелочь, иначе человека гоняют по кругу |
| `src/logic/backup.ts:shouldWriteBackup` | Когда переписывать файл. Чистая функция, под тестами |

**Чего нельзя ломать при следующих правках:**

- **Замок файла — не то же самое, что число записей.** Смена пароля или
  включение шифрования старит файл сама по себе. Без этого экран обещал
  зашифрованную копию, пока в файле лежал открытый дневник.
- **Пароль применяется целиком, а не по букве.** Экран держит черновик у себя и
  отдаёт его по уходу из поля. Иначе первая же нажатая клавиша уходила в файл
  как ключ шифрования, копия отмечалась сохранённой, и настоящий пароль в файл
  уже не попадал.
- **Неудача записи бывает двух видов.** `write` возвращает `retry` (папка
  недоступна, доступ цел) и `lost` (файл удалён, доступ отозван). Сводить их
  обратно к `false` нельзя: тогда выключенный Wi-Fi отвязывает облачную папку, и
  человек ищет файл заново после каждой поездки в метро.
- **Пустой дневник поверх копии не пишем.** Записать пустоту — потерять копию.
- **Конверт готовится в одном месте** (`envelope()` в `src/ui/useBackup.ts`) для
  всех трёх путей: автокопии, «сохранить в файл», «поделиться». Разойдясь, они
  один раз уже дали открытый дневник в облако при включённом шифровании.

**Пароль лежит в localStorage** (`omron.backup-password`), и это осознанно: он
защищает файл **в облаке**, а не телефон — дневник рядом лежит открытым. Держать
его только в памяти означало бы, что после каждого перезапуска копии молча
перестают идти.

## Люди — чего нельзя ломать

- **Владелец-призрак.** Идентификатор человека в препарате может указывать на
  удалённого. Возвращать его как есть нельзя: фильтр по человеку не найдёт
  коробку ни у кого, и она пропадёт из аптечки у всех сразу. `ownerOf` проверяет,
  что такой человек существует, и иначе отдаёт первого — ровно это обещает окно
  подтверждения при удалении.
- **Кнопка «Удалить человека» есть у каждого, включая первого.** Найденная
  поиском «первая такая кнопка» удаляет `Я`, а не второго. Меня это поймало при
  проверке; человека поймает так же.
- **Занятую память прибора второму не отдаём.** Два человека на одной памяти —
  один дневник давления на двоих, где не разобрать, чьё измерение.
- **Напоминания строятся по всей аптечке, а не по выбранному человеку.**
  Уведомление жене обязано прийти и тогда, когда открыт дневник мужа.

## Граница отложенных напоминаний

`SNOOZE_BASE` обязан лежать выше самого большого `reminderId`. Тот считается как
`((день % 2048) * 8 + человек % 8) * 1024 + приём * 8 + повтор`, то есть до
16 777 215 (с 0.7.1 в номере есть разряд человека — уведомления идут на
каждого отдельно). Прежние 900 000 потолок не покрывали: номер дня растёт на
единицу в сутки, и обычные напоминания начали бы выдавать себя за отложенные —
`cancelAll` и уборка пропускали бы их молча, а в шторке копились бы приёмы за
прошедшие дни. Теперь граница 20 000 000, и тест держит потолок под ней.

## Что проверено на устройстве 2 сентября — 0.7.1

Отладочная сборка поверх релизной 0.7.0 с сохранением данных, Mate 60 Pro,
14:17–14:40; полный протокол с номерами и значениями — в хабе рядом с APK
(`проверка-на-приборе_v0.7.1_2026-09-02.md`).

- **А1.** В 14:27 у вечерних строк «Приёма» кнопок нет, стоит «с 20:00» /
  «с 21:00», внизу «Отметить заранее»; утро — с кнопками и «Принял всё».
- **А2.** Заведён «Отец», активным возвращён «Я»; форма с «Чей препарат: Отец»
  сохранила `owner` отца при активном «Я».
- **А3.** С двумя людьми на одном слоте 20:00 очередь — 220 уведомлений, у
  каждого имя в заголовке и `person` в данных, номера разные; горизонт 14 дней.
  Тестовое уведомление отца с `actionTypeId: 'omron-dose'` → «ПРИНЯЛ» в шторке:
  списалась только его коробка, Амлодипин и Метформин «Я» на том же слоте не
  тронуты, из очереди ушли только повторы отца.
- **А4/А5.** Чужой файл (другие люди, тема, цели, часы) через «Импорт из файла»:
  новая коробка добавлена со всеми 16 полями побайтно, а people, activePerson,
  theme, цели, textScale, intakeTimes, pairingKey, userNames, sections, startTab,
  remindersOn — без изменений; 49 измерений и 0 надгробий как были.
- **А6.** В автокопии, записанной 0.7.1, `pairingKey` нет.
- **А7** на приборе не смотрел (в тестовом профиле обычный текст) — только браузер.
- Найдено и исправлено после проверки: сообщение «Настройки восстановлены»
  писалось и при чужом файле. Теперь `takesPersonalFrom` решает и слияние, и
  текст; чужой файл даёт «Люди и цели оставлены свои: файл от другой семьи».
  Проверено в браузере обоими файлами, на приборе — нет.
- **Наблюдение, не дефект выпуска:** автокопия переписывается при изменении
  *числа* записей (`count` в `useBackup`), а не полей — «Принял» из шторки
  копию не обновил. Остаток и отметки попадут в файл при следующем добавлении
  или удалении записи.
- **Разбор скептиков после проверки** (четыре линзы, три опровергателя на
  находку). Закрыто кодом до выпуска: (1) сообщение «Настройки восстановлены»
  писалось и при чужом файле — теперь `takesPersonalFrom` решает и слияние, и
  текст; (2) чужой файл с тем же `p1` сходил за свой — для `p1` требуется ещё
  и то же имя, а «семья не заведена» определяется по составу (один человек
  «Я»), не по идентификатору; (3) карточка, поставленная ещё 0.7.0, без
  `person` при нескольких людях отмечала всех — теперь открывает «Приём»;
  (4) людей в дневнике не больше `MAX_PEOPLE = 8` — столько различает разряд
  номера напоминания; (5) расшифровка чужой копии включала шифрование своих
  автокопий её паролем — теперь только для своей; (6) известным коробкам при
  восстановлении дописываются владелец, даты и история (`fillMissingFromCopy`)
  — для тех, кого наполняли копией до 0.7.1; (7) форма правки коробки с
  висячим владельцем показывает первого человека, а не активного. Всё это —
  typecheck, тесты и браузер (Playwright), на приборе не проверялось.
  Принято без правки: отложенное «на 15 минут» из 0.7.0 (номера 4 000 000+)
  первая же пересборка 0.7.1 снимает как «лишнее» — окно в четверть часа,
  один раз, молча. Ответ владельца (2 сентября): отец пьёт утренние таблетки
  в 9:00, текст у него «Очень крупный». Окно части суток его не задевает,
  пока утреннее время в его настройках не позже 10:00; находки консилиума
  про xlarge — его, режим `large` снимать не нужно.
- Тестовые данные убраны: «Я» один, пять засеянных коробок, 49 измерений,
  164 уведомления, тестовые файлы удалены.

## Что проверено на устройстве 1 сентября

Mate 60 Pro, Android 15, вся цепочка целиком:

- выбор файла в «Загрузках» — SAF-окно предлагает и Облако@Mail.ru, и локальные
  папки; файл записан сразу после выбора, 14 037 байт;
- шифрование: пароль набран по буквам — в файл не ушёл ни один промежуточный
  вариант; применён по уходу из поля — файл перезаписан конвертом, 18 892 байта;
- смена пароля при неизменном числе записей перезаписала файл (соль другая);
- восстановление зашифрованной копии — 49 записей, настройки на месте;
- неверный пароль → «Не подошёл пароль», не «файл повреждён»;
- `check()` на чужой URI → `ok:false`, на свой → `ok:true` с именем;
- удалённый файл провайдер «Загрузок» создаёт заново при записи — сценарий
  `lost` там не воспроизводится, он проверен через `check()` напрямую;
- вывод ключа из пароля — 43–62 мс.

**Перезагрузка проверена 1 сентября.** После перезапуска телефона доступ к файлу
копий цел (`check` вернул имя файла), запись проходит, напоминания на месте —
156 штук. Не проверено только `pm clear`: это стёрло бы настоящий дневник.

## Проверки

`npm run typecheck` чист, все автопроверки проходят. Percy: тринадцать
экранов в двух темах, среди них «Настройки — пароль копии» и «Настройки —
история версий». Последний прогон — сборка №12 после 0.7.1.

## Грабли — чтобы не наступить снова

- **Помощники для телефона живут в scratchpad и пропадают между сессиями.**
  Пересозданы в `scratchpad/tools/`: `run.sh` (JS в приложение через CDP,
  python `websocket` с `suppress_origin=True`), `install.sh` (проходит окна
  EMUI: «Позже», «Продолжить», «Установить»), `tap.sh` (касание по подписи
  через uiautomator). На релизной сборке CDP недоступен — ставить отладочную.
- **Отметка приёма подтверждает расчётный остаток целиком.** На живом дневнике
  «Принял» уронил остаток Метформина с 44 до 22 — это не дефект: 44 были
  подтверждены пять дней назад, расписание с тех пор списало 20, и экран уже
  показывал ≈22. Проверяя отметки на настоящем дневнике, восстанавливать надо
  и `left`, и `leftAt`.
- **Кнопка «Назад» на приборе проверяется тремя способами**: `input keyevent 4`,
  жест `input swipe` от левого края (targetSdk 36, предиктивный «назад») и
  `history.length` в WebView — он обязан оставаться единицей, иначе где-то
  завелась вторая история.
- **Ввод давления — барабаны, а не поля**: `input[inputmode=numeric]` на экране
  «Давление» нет вовсе, автоматизировать ввод через них не выйдет.
- **Программный `click()` через CDP не считается нажатием человека.** Буфер
  обмена и «поделиться» требуют жеста: проверять их надо `adb shell input tap`
  по координатам из дампа, иначе получите ложный отказ.
- **Ссылку из приложения перехватывает приложение сети.** У аптек включены
  «ссылки приложения» на свой домен: `<a href>` в WebView уходит системе, и
  Аптека.ру открывается на своей главной, теряя запрос. Лечится нативным
  `openInBrowser` в `SystemSettings.java`; проверять обязательно на телефоне,
  где это приложение установлено, и с холодного старта.
- **Узлы WebView ниже сгиба в дампе uiautomator имеют границы [0,0][0,0].**
  Прокрутите к элементу перед нажатием, иначе `tap.sh` промахивается молча.
- **В шторке EMUI кнопки уведомления («Принял», «Отложить») скрыты, пока
  уведомление не раскрыто** — свайп вниз по самому уведомлению, потом
  `tap.sh "ПРИНЯЛ"` (подписи в дампе uiautomator — заглавными).
- **`tap.sh` берёт самое широкое совпадение по подписи**: у окна установки
  AppGallery пять чипов «УСТАНОВИТЬ» в рекомендациях выше главной кнопки, и
  первое совпадение — чип.
- **Файл в `<input type=file>` без системного выбора** — `tools/setfile.py`
  (CDP `DOM.setFileInputFiles`) с путём в кэше приложения. Туда файл кладётся
  через `adb shell "run-as <пакет> sh -c 'cat > ./cache/x.json'" < файл`:
  `run-as` не читает `/sdcard`, а WebView не читает чужие файлы в `Download`.
- **У «Я» на тестовом телефоне идентификатор не `p1`** (человек пересоздавался
  1 сентября). «Семья не заведена» поэтому считается по составу — один человек
  с именем «Я», — а `p1` при сверке файла требует ещё и совпадения имени: он
  есть на каждой установке. Настоящее лечение — уникальные идентификаторы из
  части В5 плана (BACKLOG §9).
- **Кириллические имена в тестах сталкиваются между блоками** одного файла
  (`один`, `в`, `день`, `набор`) — новые блоки писать с уникальным префиксом.
- **Нижняя граница горизонта напоминаний относится к расчётному потолку, а не
  к заданному `horizonDays`** — иначе тесты с `horizonDays: 1` получают три
  дня и всё троится.
- **Столбик и чипы переключателей — явные модификаторы `segmented--stack` и
  `segmented--chips`, не `:has()`**: селектор по содержимому цеплял и полосу
  людей. `:has()` в CSS остаётся в четырёх местах (бейдж с галкой, поле даты,
  таблица измерений) — Chrome WebView с 105-й версии, у аудитории новее.

- **Gradle виснет** на сетевой проверке зависимостей. `./gradlew --stop`, потом
  сборка с `--offline` — секунды вместо минут.
- **`adb shell` съедает стандартный ввод** — каждому вызову в скриптах нужен
  `</dev/null`, иначе до python доходит пустая строка.
- **Кириллические имена переменных в bash не подставляются** — `$ОТВЕТ` пуст, и
  проверка «срабатывает» вхолостую.
- **`am force-stop` снимает будильники приложения** — для проверки уведомлений
  нужен `am kill`.
- **Установку перекрывают два окна EMUI** — «Внимание» с «Продолжить» и проверка
  AppGallery с «Установить». Без них `adb install` висит до таймаута.
- **React слушает `focusout`, а не `blur`** — в проверках через CDP отправлять
  надо `new FocusEvent('focusout', { bubbles: true })`, иначе `onBlur` не
  срабатывает и правка выглядит сломанной, хотя работает.
- **Координаты из `getBoundingClientRect` не годятся для `adb shell input tap`** —
  в них нет высоты строки состояния, и касание уходит выше цели примерно на
  60 точек. Снимать координаты надо со скриншота.
- **EMUI встревает в установку третьим окном** — «Обновление ПО» с предложением
  обновить прошивку. Оно перекрывает окно установщика, и `adb install` висит.
  Жать «Позже»: обновление прошивки — не наше решение.
- **Записи с прибора лежат под обеими памятями.** В настоящем дневнике их 45 под
  первой и 4 под второй. До появления людей вторые были не видны никому.
- **`Reveal` прячет содержимое визуально** — свёрнутый блок остаётся в
  `innerText`. Судить о состоянии по тексту нельзя, надо смотреть `data-open`.
