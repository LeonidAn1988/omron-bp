import type { CapacitorConfig } from '@capacitor/cli'

/**
 * Сборка приложения под Android.
 *
 * Веб-версия и Android собираются из одного исходника: Capacitor берёт готовый
 * `dist/` и кладёт его внутрь APK. Ядро и интерфейс при этом не меняются —
 * различаются только реализации портов в `src/platform/`.
 */
const config: CapacitorConfig = {
  /**
   * ВРЕМЕННЫЙ идентификатор. Заменить на доменный до первой подачи в магазин.
   *
   * Пространство `io.github.leonidan1988` не выдумано — оно действительно наше,
   * поэтому случайная публикация с ним не станет захватом чужого имени. Но
   * после публикации идентификатор не меняется никогда: другой идентификатор —
   * это другое приложение, без отзывов и без обновлений у тех, кто установил.
   * Поэтому переименование обязано случиться раньше подачи, а не после.
   */
  appId: 'io.github.leonidan1988.omronbp',

  /**
   * Подпись под значком на телефоне. В магазине имя отдельное и ограничено
   * тридцатью символами без спецсимволов — нынешнее «Дневник здоровья ·
   * давление и сахар» туда не влезает и содержит запрещённую точку-разделитель.
   */
  appName: 'Дневник здоровья',

  webDir: 'dist',

  plugins: {
    /**
     * Окно выбора прибора рисует плагин, и по умолчанию оно на английском:
     * «Scanning…», «Cancel», «No device found». Проверено на телефоне — именно
     * так его и видит человек.
     *
     * Для приложения, которым пользуется пожилой человек, это брак: он нажимает
     * «Подключить и выгрузить» и упирается в чужой язык ровно в тот момент,
     * когда от него требуется выбрать нужную строку.
     *
     * «Ищу тонометр», а не «Сканирование»: человек ищет прибор, а не выполняет
     * операцию сканирования.
     */
    BluetoothLe: {
      displayStrings: {
        scanning: 'Ищу тонометр…',
        cancel: 'Отмена',
        availableDevices: 'Найденные приборы',
        noDeviceFound: 'Ничего не нашлось. Нажмите кнопку Bluetooth на тонометре и попробуйте снова.',
      },
    },
  },

  android: {
    /**
     * Отладка содержимого WebView через Chrome DevTools.
     *
     * Нужна при работе с прибором: логи обмена по Bluetooth видны только изнутри
     * страницы, снаружи их не достать.
     *
     * Но оставленная в релизе, она открывает весь дневник любому, кто получил
     * телефон и кабель: через `chrome://inspect` читается хранилище целиком, без
     * рута и без пароля. Проверено на своём же устройстве — именно так шла вся
     * отладка этого вечера.
     *
     * Поэтому решает не комментарий «не забыть выключить», а переменная сборки.
     * Для магазина: `OMRON_RELEASE=1 npx cap sync android`.
     */
    webContentsDebuggingEnabled: process.env.OMRON_RELEASE !== '1',
  },
}

export default config
